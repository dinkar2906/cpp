#include <bits/stdc++.h>
using namespace std;

using intt = long long;

void solve() {
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    intt n;
    cin>>n;
    

    // even after this methof of sigma(d=1 to n(d*floor(n/d)))---->test size 10^12 still 0(n) not good
    // Harmonic Series 


    return 0;
}