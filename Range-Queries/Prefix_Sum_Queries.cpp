#include <bits/stdc++.h>
using namespace std;

using intt = long long;

void solve() {
    intt n,q;
    cin>>n>>q;

    vector<intt> vec(n);

    for (intt i = 0; i < n; i++)
    {
        cin>>vec[i];
    }
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    // cin >> t;

    while (t--) solve();

    return 0;
}